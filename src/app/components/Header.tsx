import React from 'react';

const Header: React.FC = () => {
  return (
    <header>
      <h1>Search Query Maker</h1>
      {/* Otros elementos del encabezado aquí */}
    </header>
  );
};

export default Header;